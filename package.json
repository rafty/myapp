{
  "name": "myapp",
  "version": "0.1.0",
  "bin": {
    "myapp": "bin/myapp.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "synth": "npm run build && cdk synth",
    "diff": "npm run build && cdk diff",
    "lint": "tsc --noEmit",
    "deploy:sbx": "npm run build && cdk deploy -c stage=sbx",
    "deploy:dev": "npm run build && cdk deploy -c stage=dev",
    "deploy:stg": "npm run build && cdk deploy -c stage=stg",
    "deploy:prod": "npm run build && cdk deploy -c stage=prod",
    "cdk-nag-run": "node -e \"console.log('cdk-nag-run: TODO add cdk-nag in Phase 6')\""
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^24.10.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "cdk-nag": "^2.28.146",
    "aws-cdk": "2.1033.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.215.0",
    "constructs": "^10.0.0"
  }
}
